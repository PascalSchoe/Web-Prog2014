
function macheMeldung(seite, meldung)
{
   /* var seite = window.open(seite);
    window.location=seite;
    seite.addEventListener("load",function(){
        $("serverMeldung").innerHTML="test";
        wechselSichtbarkeit("serverMeldung");
    },true);
    */
    window.location=seite;
    alert(meldung);
    
 
}
